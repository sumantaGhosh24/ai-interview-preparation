import ModeToggle from "@/components/mode-toggle";

export const metadata = {
  title: "Home",
};

const Home = () => {
  return (
    <div>
      <h1>Home</h1>
      <ModeToggle />
    </div>
  );
};

export default Home;
